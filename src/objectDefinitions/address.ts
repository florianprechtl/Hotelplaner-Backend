export type AddressObject = {
  street: string;
  postCode: string;
  city: string;
  country: string;
  notes: string;
};

export interface Address {
  street: string;
  postCode: string;
  city: string;
  country: string;
  notes: string;
}
